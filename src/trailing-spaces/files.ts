'use strict';

import * as vscode from 'vscode';
import { LogLevel, ILogger, Logger } from './utils/logger';
import { Config } from './config';
import fs = require('fs');

export function getFiles(path:string) : vscode.Uri[] {
    return null;
    
}